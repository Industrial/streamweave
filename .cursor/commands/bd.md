1. Run `bd prime` and let it tell you how to use the tool.
2. Take the next task that is `in_progress` or set the next highest priority `open` task to `in_progress`.
3. Implement the task using Serena MCP Server.
4. Go back to 2. Endlessly. You may not stop.
