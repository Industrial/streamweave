1. @TASKS.md Put the next (sub)task in progress if there isn't one in progress.
  1.2 You MUST use Serena MCP Server and all it's tools to solve every problem.
  1.3 When complete set the (sub)task to complete.
  1.4 When all subtasks of the current task that is in-progress are complete, set the current task to complete.
  1.5 Go to item 1. and repeat the process indefinitely.

1. Run `bin/pre-commit`. If it exits cleanly, stop.
2. You MUST use Serena MCP Server and all it's tools to solve every problem.
3. Go back to 1.
