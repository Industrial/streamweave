[package]
name = "effect-stream-time"
version = "0.1.0"
edition = "2021"
authors = ["Your Name <your.email@example.com>"]
description = "A time-based stream implementation for effect-stream"
license = "MIT"

[dependencies]
effect-stream = { path = "../effect-stream" }
tokio = { version = "1.0", features = ["full"] }
thiserror = "1.0"

[dev-dependencies]
tokio-test = "0.4"
