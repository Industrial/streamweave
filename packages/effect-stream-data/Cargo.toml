[package]
name = "effect-stream-data"
version = "0.1.0"
edition = "2021"
authors = ["StreamWeave"]
description = "Data transformation streams for effect-stream"
license = "MIT"
readme = "README.md"

[dependencies]
effect-stream = { path = "../effect-stream" }
tokio = { version = "1.36", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
