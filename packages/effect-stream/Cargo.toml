[package]
name = "effect-stream"
version = "0.1.0"
edition = "2021"
authors = ["Your Name <your.email@example.com>"]
description = "Streaming capabilities for the Effect type system"
license = "MIT"

[dependencies]
effect-core = { path = "../effect-core" }
effect-resource = { path = "../effect-resource" }
futures = "0.3"
tokio = { version = "1.0", features = ["full"] }
tokio-stream = "0.1"
async-trait = "0.1"
