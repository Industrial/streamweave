[package]
name = "effect-stream"
version = "0.1.0"
edition = "2021"
description = "A stream implementation for handling effects and errors"
authors = ["Your Name <your.email@example.com>"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/yourusername/streamweave"

[dependencies]
effect-core = { path = "../effect-core" }
futures = "0.3"
thiserror = "1.0"
tokio = { version = "1.36", features = ["full"] }

[dev-dependencies]
tokio-test = "0.4"
