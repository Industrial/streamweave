{
  "name": "streamweave-wasm-browser-example",
  "version": "0.2.1",
  "type": "module",
  "description": "StreamWeave WASM browser example",
  "scripts": {
    "build:wasm": "wasm-pack build --target web --out-dir pkg",
    "dev": "vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "streamweave": {
      "path": "../../",
      "features": ["wasm"],
      "default-features": false
    }
  }
}

