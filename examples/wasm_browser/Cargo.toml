[package]
name = "wasm-browser-example"
version = "0.2.2"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
streamweave = { path = "../../", features = ["wasm"], default-features = false }
wasm-bindgen = "0.2"

