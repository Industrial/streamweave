#!/usr/bin/env bash
set -euo pipefail
exec > >(tee log/build-wasm.log) 2>&1 

echo "> build-wasm"

echo "> build-wasm > cargo build"
RUSTFLAGS='-D warnings' cargo build \
  --target wasm32-unknown-unknown \
  --features wasm \
  --no-default-features \
  --release \
  --lib
