#!/usr/bin/env bash
# Generate docs for all packages

set -euo pipefail
exec > >(tee log/docs.log) 2>&1

echo "> docs"

echo "> docs > cargo doc --workspace"
devenv shell -- cargo doc --workspace --no-deps --all-features
