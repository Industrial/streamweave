//! Distinct transformer module.
//!
//! This module provides functionality for removing duplicate items from a stream,
//! keeping only distinct values.

/// The distinct transformer implementation.
pub mod distinct_transformer;
/// Input types for the distinct transformer.
pub mod input;
/// Output types for the distinct transformer.
pub mod output;
/// Transformer trait implementation for distinct.
pub mod transformer;
