//! Split at transformer module.
//!
//! This module provides functionality for splitting a stream of items at a specific index.

/// Input types for the split at transformer.
pub mod input;
/// Output types for the split at transformer.
pub mod output;
/// The split at transformer implementation.
pub mod split_at_transformer;
/// Transformer trait implementation for split at.
pub mod transformer;
