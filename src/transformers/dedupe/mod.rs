//! Dedupe transformer module.
//!
//! This module provides functionality for removing duplicate items from a stream.

/// The dedupe transformer implementation.
pub mod dedupe_transformer;
/// Input types for the dedupe transformer.
pub mod input;
/// Output types for the dedupe transformer.
pub mod output;
/// Transformer trait implementation for dedupe.
pub mod transformer;
