//! Vec consumer module.
//!
//! This module provides the `VecConsumer` which collects items into a `Vec`.

/// Consumer trait implementation for Vec.
pub mod consumer;
/// Input type definitions for the Vec consumer.
pub mod input;
/// The Vec consumer implementation.
pub mod vec_consumer;
