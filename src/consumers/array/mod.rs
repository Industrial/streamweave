//! Array consumer module.
//!
//! This module provides a consumer that collects stream items into a fixed-size array.

/// The array consumer implementation.
pub mod array_consumer;
/// Consumer trait implementation for array.
pub mod consumer;
/// Input types for the array consumer.
pub mod input;
