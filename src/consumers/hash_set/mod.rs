//! HashSet consumer module.
//!
//! This module provides the `HashSetConsumer` which collects items into a `HashSet`.

/// Consumer trait implementation for HashSet.
pub mod consumer;
/// The HashSet consumer implementation.
pub mod hash_set_consumer;
/// Input type definitions for the HashSet consumer.
pub mod input;
