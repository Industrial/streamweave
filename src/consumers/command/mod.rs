//! Command consumer module.
//!
//! This module provides the `CommandConsumer` which executes an external command for each item.

/// The Command consumer implementation.
pub mod command_consumer;
/// Consumer trait implementation for Command.
pub mod consumer;
/// Input type definitions for the Command consumer.
pub mod input;
