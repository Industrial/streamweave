//! Array producer module.
//!
//! This module provides the `ArrayProducer` which generates a stream from the elements of an array.

/// The Array producer implementation.
pub mod array_producer;
/// Output type definitions for the Array producer.
pub mod output;
/// Producer trait implementation for Array.
pub mod producer;
